import "./loginBlock.scss";

const LoginBlock = () => {
  return (
    <div className="login">
      {/* <a href="#" className="login__button login__button-app">
        {t("login.getTheApp")}
      </a>
      <p>{t("login.or")}</p>
      <button className="login__button login__button-login">{t("login.login")}</button> */}
    </div>
  );
};

export default LoginBlock;
